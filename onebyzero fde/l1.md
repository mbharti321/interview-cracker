- how many documents in rag pipeline?
- how do you fetch data for user query?
- if documents count increases from 1k to 10k, will you do similarity search for all 10k docs?
  - how will you optimize the vector db search?
    - use tag and filter based on projects/ document category while ingestion and while making similarity search to avoid searching all docs
- how to pass data between agents
- hosted where?
- how agents communicate?
- how one agent pass information to another agent or orchestrator agent in LanGraph?
- What data do you store in global state in Langraph?

---------------
# Coding problem:
```py
"""
Exercise 1 — Structured Extraction  (~20 min)
─────────────────────────────────────────────
Given a raw product review, call an LLM and extract structured fields from it.

TASK
────
1. Define a Pydantic model `ReviewAnalysis` with these fields:
     - sentiment:      Literal["positive", "negative", "neutral"]
     - rating:         int  (1–5, your best estimate)
     - key_features:   list[str]  (up to 5 things the reviewer mentions)
     - summary:        str  (one sentence)

2. Implement:
     extract_review(text: str) -> ReviewAnalysis

   Requirements:
   • Call an LLM (use ANTHROPIC_API_KEY or OPENAI_API_KEY).
   • The LLM must return valid JSON that maps to ReviewAnalysis.
   • If parsing fails, retry up to MAX_RETRIES times.
   • Raise ValueError after exhausting retries.

3. Run main() to verify your solution against the sample review below.

HINTS
─────
• Ask the LLM to respond with JSON only — no prose, no markdown fences.
• Include the expected schema in your prompt so the model knows the shape.
• temperature=0 gives more deterministic extraction.

SAMPLE REVIEW (use this in main())
───────────────────────────────────
"Absolutely love this espresso machine. Setup took 5 minutes and the
 shots are consistently great. The built-in grinder is a bit loud but
 the milk frother is fantastic. Only downside: the drip tray fills up
 fast. Would 100% recommend to any home barista."
"""
import os
from pydantic import BaseModel
from typing import Literal

MAX_RETRIES = 3

class LLMService:
    # cons
    def ask_llm(query):
        # make llm api call
        pass
        
    

class ReviewAnalysis(BaseModel):
    sentiment:     Literal["positive", "negative", "neutral"]
    rating:        int
    key_features:  list[str]
    summary:       str

review_prompt = f'''You are given a review and your task is to analyse it and return the extraced information in a json format.
Returned format:{ReviewAnalysis}
And if the information is n

'''


def extract_review(review: str) -> ReviewAnalysis:
    """Call an LLM and return a parsed ReviewAnalysis. Retry up to MAX_RETRIES on failure."""
    llm_service = LLMService()
    
    # clean review
    
    try:        
        llm_response = llm_service.ask_llm(review)
        
        # load as json
        json_respons
        
    except:
        raise NotImplementedError


def main():
    review = (
        "Absolutely love this espresso machine. Setup took 5 minutes and the "
        "shots are consistently great. The built-in grinder is a bit loud but "
        "the milk frother is fantastic. Only downside: the drip tray fills up "
        "fast. Would 100% recommend to any home barista."
    )
    result = extract_review(review)
    print(result.model_dump_json(indent=2))


if __name__ == "__main__":
    main()
 

```