1. Write a Python function `preprocess_text(text: str) -> str` that takes a string as input and performs the following sanitization steps: 1. Remove all non-alphanumeric characters (except spaces). 2. Replace multiple spaces with a single space. 3. Trim leading/trailing spaces. This function is intended for basic text normalization before feeding to an LLM.
2. Using FastAPI and Pydantic, define a Pydantic model `ItemResponse` with an `item_id` (integer), `name` (string), and an optional `query_param` (string). Then, create a FastAPI `GET` endpoint `/items/{item_id}` that takes an `item_id` (integer path parameter) and an optional `q` (string query parameter), returning a JSON response based on the `ItemResponse` model with a hardcoded item name 'Example Item'.
3. Given two tables: `users` (columns: `user_id` INT, `username` VARCHAR, `created_at` TIMESTAMP) and `api_logs` (columns: `log_id` INT, `user_id` INT, `timestamp` TIMESTAMP, `endpoint` VARCHAR). Write an SQL query to find the `username` and `user_id` of all users who have made more than 5 API calls in the last 30 days (relative to the current date).
4. Create a simple React functional component in TypeScript called `GreetingCard` that accepts a `name` prop (string) and an optional `message` prop (string). The component should display 'Hello, [name]!' and if `message` is provided, it should display it below the greeting.
5. Implement a Python function `find_relevant_chunks(query_embedding: list[float], text_chunks: list[dict], k: int) -> list[str]` that simulates a RAG lookup. `query_embedding` is a list of floats. `text_chunks` is a list of dictionaries, where each dict has at least 'text' (str) and 'embedding' (list[float]). The function should calculate the cosine similarity (dot product for normalized vectors) between the `query_embedding` and each `chunk['embedding']`, then return the 'text' of the `k` most similar chunks. Assume embeddings are already normalized for simplicity.
6. Write a `Dockerfile` to containerize a Python FastAPI application. The application code is in `/app`, and dependencies are listed in `requirements.txt`. The FastAPI application runs using `uvicorn` on port 8000. Ensure best practices like using a non-root user.
7. Design a Python function `route_llm_request(prompt: str, available_models: list[str]) -> str` that implements a basic LLM routing strategy. Given a `prompt` and a list of `available_models` (e.g., `['openai-gpt4', 'anthropic-claude', 'local-llama']`), the function should choose a model based on the following rules: 1. If 'openai-gpt4' is available and the prompt length is less than 500 characters, use 'openai-gpt4'. 2. Else if 'anthropic-claude' is available and the prompt length is between 500 and 2000 characters, use 'anthropic-claude'. 3. Otherwise, if 'local-llama' is available, use 'local-llama'. 4. If no suitable model is found, raise a `ValueError`.
8. Design the core components and workflow for a 'maker-checker' system in a Python/FastAPI application, as required for compliance in regulated environments. This system ensures that a critical action (e.g., modifying a sensitive configuration, approving a transaction) initiated by one user ('maker') must be reviewed and approved by another user ('checker') before it takes effect. Describe the necessary API endpoints, database schema considerations (e.g., for tracking pending actions and audit trails), and the state transitions involved. Provide Python/pseudo-code outlines for the key logic.
9. Outline the design for an asynchronous task processing system in Python, suitable for handling heavy AI workloads as mentioned in the JD. Focus on the core components: a task producer, a task queue, and worker processes. Describe how you would ensure tasks are processed reliably, including handling transient failures (retries) and logging/observability (e.g., using OpenTelemetry for tracing). Provide a high-level Python class structure or pseudo-code for the main components.
10. Write a Python function `redact_pii(data: dict, sensitive_keys: list[str], redaction_placeholder: str = '***REDACTED***') -> dict` that takes a dictionary `data` (potentially nested), a list of `sensitive_keys` to redact, and an optional `redaction_placeholder`. The function should return a new dictionary with all values corresponding to `sensitive_keys` replaced by the placeholder. The redaction should be deep, applying to nested dictionaries and lists containing dictionaries as well. This is crucial for PII handling in regulated environments.





